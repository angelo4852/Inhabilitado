<MudDialog>
    <TitleContent>
        <div class="container">
            <p style="text-align:center; font-size: 1.2rem;">
                <b>Pasos para cargar desde un archivo de Excel</b>
            </p>
        </div>
    </TitleContent>
    <DialogContent>
        <MudText>
            <div class="container" style="text-align: justify; font-size: 1rem">
                <p>
                    <MudIcon Icon="@Icons.Material.Filled.ChevronRight" Style="color:#A8113E; font-size: 1rem"></MudIcon>
                    Haz clic en "Seleccionar Archivo" y escoge un archivo de Excel (*.xlsx) con los registros.
                </p>
                <p>
                    <MudIcon Icon="@Icons.Material.Filled.ChevronRight" Style="color:#A8113E; font-size: 1rem"></MudIcon>
                    Una vez seleccionado hacer clic en "Cargar Archivo".
                </p>
                <p>
                    <MudIcon Icon="@Icons.Material.Filled.ChevronRight" Style="color:#A8113E; font-size: 1rem"></MudIcon>
                    Espere a que se carguen todos los registros, el tiempo de carga dependerá del número de registros en el archivo.
                </p>
                <p>
                    <MudIcon Icon="@Icons.Material.Filled.ChevronRight" Style="color:#A8113E; font-size: 1rem"></MudIcon>
                    La constancia tendrá una vigencia de treinta días naturales a partir de la fecha de su expedición.
                </p>
                <p>
                    <MudIcon Icon="@Icons.Material.Filled.ChevronRight" Style="color:#A8113E; font-size: 1rem"></MudIcon>
                    Al finalizar le mostrará un mensaje con los datos cargados: procesados, insertados, no insertados
                </p>
                <p>
                    <MudIcon Icon="@Icons.Material.Filled.ChevronRight" Style="color:#A8113E; font-size: 1rem"></MudIcon>
                    Es importante no cancelar este proceso, de lo contrario la información quedará inconsistente
                </p>
            </div>
        </MudText>
    </DialogContent>
    <DialogActions>
        <MudButton Style="color:white; background-color:#A8113E; width:150px; margin-right:25px; margin-bottom: 8px" OnClick="Submit">Cerrar</MudButton>
    </DialogActions>
</MudDialog>

<style>
    a {
        color: #A5987E;
        opacity: 75%;
    }

    .my-custom-class {
        backdrop-filter: blur(10px);
    }
</style>

@code {

    [CascadingParameter] MudDialogInstance MudDialog { get; set; }

    void Submit() => MudDialog.Close(DialogResult.Ok(true));
    void Cancel() => MudDialog.Cancel();
}
