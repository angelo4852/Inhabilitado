@inject NavigationManager navigationManager
<style>

    html, body {
        background-image: url("img/loanding.svg") !important;
        background-size: cover;
        background-repeat: no-repeat;
        color: #fff;
        font-size: 100%;
        line-height: 1.5;
        font-family: Poppins, sans-serif;
        height:100vh;
        width:100vw;
    }

    .button {
        font-weight: 300;
        color: #fff;
        font-size: 1.2em;
        text-decoration: none;
        border: 1px solid #efefef;
        padding: .5em;
        border-radius: 3px;
        float: left;
        margin: 6em 0 0 -155px;
        left: 53%;
        position: relative;
        transition: all .3s linear;
    }

        .button:hover {
            background-color: var(--main-color-general-hover);
            color: #fff;
        }

    p {
        font-size: 1.4em;
        text-align: center;
        font-weight: 100;
    }

    h1 {
        text-align: center;
        font-size: 15em;
        font-weight: 100;
    }
    img{
        text-align:center;
    }

</style>

<div>
    <h1> 404</h1>
    <p>El recurso que estás intentando encontrar no está disponible en este momento.</p>
    <MudButton Class="button" Variant="Variant.Filled" Color="Color.Secondary" OnClick="PaginaPrincipal"> Ir a pagina principal</MudButton>
</div>


@code {
    private async Task PaginaPrincipal()
    {
        navigationManager.NavigateTo("/ConstanciaNoInhabilitado/Admin/Login",true);
    }
}
