@using ConstanciaNoInhabilitado.Client.Shared.Partial.Dialogs
@inject IDialogService dialogService

<MudGrid Justify="Justify.Center">
    <MudItem xs="10">
        <MudText Typo="Typo.h5">Paso II.</MudText>
        <MudText Typo="Typo.h6" Style="color:#A8113E;">Definir la Inhabilitación.</MudText>
        <br />
        <MudText Typo="Typo.subtitle1">Siga los siguientes pasos para definir la inhabilitación.</MudText>
        <br />
        <MudText Typo="Typo.body1">
            <MudIcon Icon="@Icons.Material.Filled.KeyboardArrowRight"></MudIcon>
            Haga clic en Registrar inhabilitación y capture toda la información requerida.
        </MudText>
        <MudText Typo="Typo.body1">
            <MudIcon Icon="@Icons.Material.Filled.KeyboardArrowRight"></MudIcon>
            Si faltase algun dato (Dependencias) puede cerrar la ventana (No se perderá lo que haya capturado) y de clic en "Registrar Dependencia". Aparecerá una ventana emergente la cual le permitirá registrar una nueva Dependencia.
        </MudText>
        <MudText Typo="Typo.body1">
            <MudIcon Icon="@Icons.Material.Filled.KeyboardArrowRight"></MudIcon>
            Una vez capturados todos los datos, proceda a dar clic en "Establecer Inhabilitación" y pase a la siguiente ventana
        </MudText>        
        <br />
        <MudButton Variant="Variant.Filled" Color="Color.Secondary" Style="width:15vw;" OnClick="Dependencia">Registrar Dependencia</MudButton>
        <MudButton Variant="Variant.Filled" Color="Color.Secondary" Style="width:15vw;" OnClick="Inhabilitacion">Registrar Inhabilitación</MudButton>
    </MudItem>
</MudGrid>

@code {
    public async Task Dependencia()
    {
        var options = new DialogOptions()
            {
                FullWidth = true,
                MaxWidth = MaxWidth.Small,
                DisableBackdropClick = true,
                ClassBackground = "filterBackground",
                CloseButton = true
            };
        await dialogService.ShowAsync<ModalRegistrarDependencia>("Simple Dialog", options);
    }

    public async Task Inhabilitacion()
    {
        var options = new DialogOptions()
            {
                FullWidth = true,
                MaxWidth = MaxWidth.Medium,
                DisableBackdropClick = true,
                ClassBackground = "filterBackground",
                CloseButton = true
            };
        await dialogService.ShowAsync<ModalDefineLaInhabilitación>("Simple Dialog", options);
    }
}
